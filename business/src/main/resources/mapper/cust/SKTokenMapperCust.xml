<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.train.business.mapper.cust.SkTokenMapperCust">

    <update id="decrease">
        update sk_token
        set `count` = if (`count` &lt; #{cnt}, 0, `count` - #{cnt})
        where `date` = #{date}
          and train_code = #{trainCode}
          and `count` >= #{cnt}
    </update>
</mapper>
